;;; early-init.el --- Emacs's early init file            -*- lexical-binding: t; -*-
;;; Commentary:
;;; Code:

;; Disable package.el since we use Elpaca.
(setq package-enable-at-startup nil)

(provide 'early-init)
;;; early-init.el ends here
